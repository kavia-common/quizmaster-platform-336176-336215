import React from "react";

// PUBLIC_INTERFACE
export default function Admin() {
  /** Minimal Admin page placeholder. */
  return (
    <section className="page" aria-label="Admin">
      <h1 className="pageTitle">Admin</h1>
      <p className="pageBody">
        This is a placeholder for admin tools (add/edit questions).
      </p>
    </section>
  );
}
